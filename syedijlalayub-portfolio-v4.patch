diff --git a/index.html b/index.html
index d17d694..a7d7174 100644
--- a/index.html
+++ b/index.html
@@ -14,7 +14,10 @@
   <meta property="og:type" content="website" />
   <meta property="og:url" content="https://syedijlalayub.github.io/" />
   <meta property="og:image" content="https://avatars.githubusercontent.com/u/57711072?v=4" />
+  <meta property="og:image:alt" content="Syed Ijlal Ayub — SAP Integration Consultant and Software Engineer" />
   <meta name="twitter:card" content="summary" />
+  <meta name="twitter:title" content="Syed Ijlal Ayub — Integration & Software Engineering" />
+  <meta name="twitter:description" content="Enterprise integration with a product engineer's mindset." />
   <title>Syed Ijlal Ayub — Integration & Software Engineering</title>
   <link rel="icon" href="favicon.svg" type="image/svg+xml" />
   <script type="application/ld+json">
@@ -56,7 +59,7 @@
 
       <nav class="nav-links" id="primary-nav" aria-label="Primary navigation">
         <a href="#work">Work</a>
-        <a href="#capabilities">Capabilities</a>
+        <a href="#capabilities">Approach</a>
         <a href="#experience">Experience</a>
         <a href="#about">About</a>
       </nav>
@@ -89,7 +92,7 @@
         <div class="hero-proof" aria-label="Current engineering focus">
           <div><strong>Enterprise</strong><span>SAP integration & APIs</span></div>
           <div><strong>Product</strong><span>Android · Kotlin · Compose</span></div>
-          <div><strong>Learning</strong><span>Applied machine learning</span></div>
+          <div><strong>Exploring</strong><span>Applied machine learning</span></div>
         </div>
       </div>
 
@@ -128,12 +131,12 @@
       </div>
     </section>
 
-    <section class="signal-bar" aria-label="Technical focus">
+    <section class="signal-bar" aria-label="Engineering principles at a glance">
       <div class="section-shell signal-inner">
-        <span>01 / SAP Integration</span>
-        <span>02 / API & Data Flows</span>
-        <span>03 / Kotlin & Compose</span>
-        <span>04 / Release Engineering</span>
+        <span>01 / Contracts first</span>
+        <span>02 / Explicit state</span>
+        <span>03 / Observable failure</span>
+        <span>04 / Release discipline</span>
       </div>
     </section>
 
@@ -217,29 +220,42 @@
 
     <section class="section section-muted" id="capabilities" aria-labelledby="capabilities-title">
       <div class="section-shell">
-        <div class="section-heading reveal">
-          <p class="kicker">Capabilities</p>
-          <h2 id="capabilities-title">From interface contract to user interaction.</h2>
+        <div class="section-heading split-heading reveal">
+          <div>
+            <p class="kicker">Engineering approach</p>
+            <h2 id="capabilities-title">Make the boundaries clear. Make the behavior explainable.</h2>
+          </div>
+          <p>I use the same engineering habits across integration and product work: define the contract, make state explicit, expose failure clearly, and finish the experience all the way to release.</p>
+        </div>
+
+        <div class="approach-flow reveal" aria-hidden="true">
+          <span>Business system</span><i>→</i><span>Contract</span><i>→</i><span>State</span><i>→</i><span>Behavior</span><i>→</i><span>User</span>
         </div>
 
         <div class="capability-grid">
           <article class="capability spotlight reveal">
             <span class="capability-number">01</span>
-            <h3>Integration engineering</h3>
-            <p>Interfaces, APIs, mapping, transformation, validation, operational diagnostics, and the failure paths between systems.</p>
-            <div class="capability-meta">SAP · APIs · Data flows</div>
+            <h3>Contract thinking</h3>
+            <p>Define inputs, outputs, ownership, validation, and failure semantics before complexity spreads through the implementation.</p>
+            <div class="capability-meta">Interfaces · APIs · Schemas</div>
           </article>
           <article class="capability spotlight reveal">
             <span class="capability-number">02</span>
-            <h3>Software engineering</h3>
-            <p>Maintainable application logic, state management, testing, debugging, source control, and release-oriented development.</p>
-            <div class="capability-meta">Kotlin · Python · Git · CI/CD</div>
+            <h3>State &amp; recovery</h3>
+            <p>Model lifecycle and persistence deliberately so interrupted flows, resumed games, and edge cases remain predictable.</p>
+            <div class="capability-meta">Lifecycle · Persistence · Recovery</div>
           </article>
           <article class="capability spotlight reveal">
             <span class="capability-number">03</span>
-            <h3>Product engineering</h3>
-            <p>Turning requirements into coherent behavior, with attention to UX, accessibility, persistence, edge cases, and polish.</p>
-            <div class="capability-meta">Android · Compose · Material 3</div>
+            <h3>Diagnostics by design</h3>
+            <p>Make it possible to answer what happened, where it failed, and what can safely happen next without guesswork.</p>
+            <div class="capability-meta">Tracing · Validation · Failure paths</div>
+          </article>
+          <article class="capability spotlight reveal">
+            <span class="capability-number">04</span>
+            <h3>Finish the product</h3>
+            <p>Treat accessibility, interaction feedback, regression checks, and release behavior as part of engineering correctness.</p>
+            <div class="capability-meta">UX · Accessibility · Release quality</div>
           </article>
         </div>
       </div>
@@ -302,9 +318,9 @@
         </div>
 
         <div class="principles reveal" aria-label="Engineering principles">
-          <div class="principle"><span>01</span><div><h3>Make failure behavior explicit.</h3><p>Systems are defined as much by recovery paths as by success paths.</p></div></div>
-          <div class="principle"><span>02</span><div><h3>Prefer clarity over cleverness.</h3><p>Readable code and visible system behavior make future changes safer.</p></div></div>
-          <div class="principle"><span>03</span><div><h3>Treat UX details as engineering details.</h3><p>Accessibility, feedback, state, and timing all affect correctness in practice.</p></div></div>
+          <div class="principle"><span>01</span><div><h3>Read the system before rewriting it.</h3><p>Understanding existing behavior prevents elegant fixes that break real assumptions.</p></div></div>
+          <div class="principle"><span>02</span><div><h3>Test the path nobody wants.</h3><p>Retries, interruptions, stale state, and failures deserve the same design attention as success.</p></div></div>
+          <div class="principle"><span>03</span><div><h3>Leave the next change easier.</h3><p>Clear naming, visible state, and focused boundaries are part of the deliverable.</p></div></div>
         </div>
       </div>
 
@@ -320,8 +336,8 @@
       <div class="section-shell contact-inner reveal">
         <div class="contact-index" aria-hidden="true">04</div>
         <p class="kicker">Contact</p>
-        <h2 id="contact-title">Building something that needs to <span class="accent-word">connect, behave, or scale?</span></h2>
-        <p>My public work lives on GitHub. For professional context, this site is the concise version of what I build and how I think.</p>
+        <h2 id="contact-title">If the hard part lives between <span class="accent-word">systems, state, and users,</span> I’m interested.</h2>
+        <p>My public work lives on GitHub. This site is the concise version of how I approach integration, software, and product engineering.</p>
         <div class="contact-actions">
           <a class="button button-primary" href="https://github.com/SyedIjlalAyub" target="_blank" rel="noreferrer">Open GitHub <span aria-hidden="true">↗</span></a>
           <button class="button button-ghost copy-link" type="button" data-copy="https://syedijlalayub.github.io/">Copy site link</button>
diff --git a/script.js b/script.js
index da5d058..1df127d 100644
--- a/script.js
+++ b/script.js
@@ -14,7 +14,8 @@
     savedTheme = null;
   }
 
-  const preferredLight = window.matchMedia('(prefers-color-scheme: light)').matches;
+  const colorScheme = window.matchMedia('(prefers-color-scheme: light)');
+  const preferredLight = colorScheme.matches;
 
   const setTheme = (theme, persist = true) => {
     root.dataset.theme = theme;
@@ -31,8 +32,13 @@
     setTheme(root.dataset.theme === 'dark' ? 'light' : 'dark');
   });
 
+  colorScheme.addEventListener?.('change', (event) => {
+    if (!savedTheme) setTheme(event.matches ? 'light' : 'dark', false);
+  });
+
   const closeMenu = () => {
     nav?.classList.remove('is-open');
+    document.body.classList.remove('nav-open');
     menuToggle?.setAttribute('aria-expanded', 'false');
     menuToggle?.setAttribute('aria-label', 'Open navigation');
   };
@@ -42,6 +48,7 @@
     menuToggle.setAttribute('aria-expanded', String(!open));
     menuToggle.setAttribute('aria-label', open ? 'Open navigation' : 'Close navigation');
     nav?.classList.toggle('is-open', !open);
+    document.body.classList.toggle('nav-open', !open);
   });
 
   nav?.querySelectorAll('a').forEach((link) => link.addEventListener('click', closeMenu));
@@ -130,14 +137,34 @@
   const toast = document.querySelector('.toast');
   let toastTimer;
 
+  const copyText = async (value) => {
+    if (navigator.clipboard?.writeText) {
+      await navigator.clipboard.writeText(value);
+      return;
+    }
+    const textarea = document.createElement('textarea');
+    textarea.value = value;
+    textarea.setAttribute('readonly', '');
+    textarea.style.position = 'fixed';
+    textarea.style.opacity = '0';
+    document.body.appendChild(textarea);
+    textarea.select();
+    const copied = document.execCommand('copy');
+    textarea.remove();
+    if (!copied) throw new Error('Copy unavailable');
+  };
+
   copyButton?.addEventListener('click', async () => {
+    const value = copyButton.dataset.copy || window.location.href;
     try {
-      await navigator.clipboard.writeText(copyButton.dataset.copy || window.location.href);
+      await copyText(value);
       toast?.classList.add('is-visible');
       clearTimeout(toastTimer);
       toastTimer = setTimeout(() => toast?.classList.remove('is-visible'), 1800);
     } catch {
-      copyButton.textContent = 'syedijlalayub.github.io';
+      const original = copyButton.textContent;
+      copyButton.textContent = 'Copy unavailable';
+      setTimeout(() => { copyButton.textContent = original; }, 1800);
     }
   });
 })();
diff --git a/styles.css b/styles.css
index f48db88..15deadd 100644
--- a/styles.css
+++ b/styles.css
@@ -45,6 +45,20 @@ body {
   line-height: 1.6;
   -webkit-font-smoothing: antialiased;
   text-rendering: optimizeLegibility;
+  position: relative;
+}
+body::before {
+  content: "";
+  position: fixed;
+  inset: 0;
+  z-index: -1;
+  pointer-events: none;
+  background-image:
+    linear-gradient(to right, color-mix(in srgb, var(--line) 28%, transparent) 1px, transparent 1px),
+    linear-gradient(to bottom, color-mix(in srgb, var(--line) 22%, transparent) 1px, transparent 1px);
+  background-size: 64px 64px;
+  mask-image: linear-gradient(to bottom, rgba(0,0,0,.42), transparent 68%);
+  opacity: .26;
 }
 
 ::selection { background: var(--accent); color: var(--accent-ink); }
@@ -182,8 +196,8 @@ html[data-theme="light"] .icon-moon { display: block; }
 .split-heading > p { max-width: 500px; padding-bottom: 5px; }
 
 .work-stack { display: grid; gap: 18px; }
-.case-card { display: grid; grid-template-columns: 66px 1fr; border: 1px solid var(--line); border-radius: var(--radius); background: var(--bg-elevated); overflow: hidden; transition: border-color .25s ease, transform .25s ease; }
-.case-card:hover { transform: translateY(-3px); border-color: color-mix(in srgb, var(--accent) 28%, var(--line)); }
+.case-card { display: grid; grid-template-columns: 66px 1fr; border: 1px solid var(--line); border-radius: var(--radius); background: linear-gradient(145deg, var(--bg-elevated), color-mix(in srgb, var(--accent) 1.8%, var(--bg-elevated))); overflow: hidden; transition: border-color .25s ease, transform .25s ease, box-shadow .25s ease; }
+.case-card:hover { transform: translateY(-3px); border-color: color-mix(in srgb, var(--accent) 28%, var(--line)); box-shadow: 0 22px 60px rgba(0,0,0,.12); }
 .case-rail { padding: 25px 0; display: flex; flex-direction: column; align-items: center; gap: 24px; border-right: 1px solid var(--line); color: var(--muted-2); font: 700 9px/1 ui-monospace, SFMono-Regular, Menlo, monospace; letter-spacing: .08em; text-transform: uppercase; }
 .case-rail span:last-child { writing-mode: vertical-rl; transform: rotate(180deg); }
 .case-content { padding: 32px; }
@@ -223,13 +237,27 @@ html[data-theme="light"] .icon-moon { display: block; }
 .tag-row span { padding: 7px 10px; border-radius: 8px; background: var(--bg-soft); color: var(--muted); font: 650 10px/1 ui-monospace, SFMono-Regular, Menlo, monospace; }
 
 .section-muted { border-block: 1px solid var(--line); background: var(--bg-elevated); }
-.capability-grid { display: grid; grid-template-columns: repeat(3, 1fr); border: 1px solid var(--line); border-radius: var(--radius); overflow: hidden; }
-.capability { min-height: 310px; padding: 28px; display: flex; flex-direction: column; background: var(--bg); }
+.approach-flow {
+  display: flex;
+  align-items: center;
+  gap: 13px;
+  margin: -15px 0 24px;
+  color: var(--muted-2);
+  font: 700 9px/1 ui-monospace, SFMono-Regular, Menlo, monospace;
+  letter-spacing: .07em;
+  text-transform: uppercase;
+}
+.approach-flow i { color: var(--accent); font-style: normal; }
+.capability-grid { display: grid; grid-template-columns: repeat(4, 1fr); border: 1px solid var(--line); border-radius: var(--radius); overflow: hidden; }
+.capability { min-height: 330px; padding: 28px; display: flex; flex-direction: column; background: var(--bg); transition: background .25s ease; }
 .capability + .capability { border-left: 1px solid var(--line); }
-.capability-number { color: var(--accent); font: 700 10px/1 ui-monospace, SFMono-Regular, Menlo, monospace; }
-.capability h3 { margin: 62px 0 12px; font-size: 22px; letter-spacing: -.025em; }
-.capability p { margin: 0; color: var(--muted); font-size: 14px; }
-.capability-meta { margin-top: auto; padding-top: 28px; color: var(--muted-2); font: 650 10px/1.5 ui-monospace, SFMono-Regular, Menlo, monospace; }
+.capability:hover { background: color-mix(in srgb, var(--accent) 2.5%, var(--bg)); }
+.capability-number { display: inline-flex; align-items: center; gap: 9px; color: var(--accent); font: 700 10px/1 ui-monospace, SFMono-Regular, Menlo, monospace; }
+.capability-number::after { content: ""; width: 24px; height: 1px; background: color-mix(in srgb, var(--accent) 48%, transparent); transition: width .25s ease; }
+.capability:hover .capability-number::after { width: 42px; }
+.capability h3 { margin: 58px 0 12px; font-size: 21px; letter-spacing: -.025em; }
+.capability p { margin: 0; color: var(--muted); font-size: 13px; }
+.capability-meta { margin-top: auto; padding-top: 28px; color: var(--muted-2); font: 650 9px/1.5 ui-monospace, SFMono-Regular, Menlo, monospace; text-transform: uppercase; letter-spacing: .04em; }
 
 .focus-wrap { padding-block: 26px 112px; }
 .focus-panel { display: grid; grid-template-columns: .7fr 1.3fr; gap: 60px; padding: 30px; border: 1px solid var(--line); border-radius: var(--radius); background: linear-gradient(135deg, color-mix(in srgb, var(--accent) 4%, var(--bg-elevated)), var(--bg-elevated)); overflow: hidden; }
@@ -249,7 +277,8 @@ html[data-theme="light"] .icon-moon { display: block; }
 
 .experience-layout { display: grid; grid-template-columns: .84fr 1.16fr; gap: 90px; }
 .experience-layout .section-heading > p:last-child { margin-top: 20px; }
-.timeline { border-top: 1px solid var(--line); }
+.timeline { position: relative; border-top: 1px solid var(--line); }
+.timeline::before { content: ""; position: absolute; left: -1px; top: 0; bottom: 0; width: 1px; background: linear-gradient(to bottom, var(--line-strong), var(--line), transparent); }
 .timeline-item { position: relative; display: grid; grid-template-columns: 135px 1fr; gap: 28px; padding: 27px 0; border-bottom: 1px solid var(--line); }
 .timeline-item::before { content: ""; position: absolute; left: -5px; top: 36px; width: 8px; height: 8px; border-radius: 50%; background: var(--muted-2); }
 .timeline-item.current::before { background: var(--accent); box-shadow: 0 0 0 5px color-mix(in srgb, var(--accent) 11%, transparent); }
@@ -303,10 +332,12 @@ html[data-theme="light"] .icon-moon { display: block; }
   .split-heading > p { max-width: 690px; }
   .case-grid { grid-template-columns: 1fr; }
   .puzzle-board { max-width: 650px; }
-  .capability-grid { grid-template-columns: 1fr; }
-  .capability { min-height: 240px; }
-  .capability + .capability { border-left: 0; border-top: 1px solid var(--line); }
-  .capability h3 { margin-top: 34px; }
+  .capability-grid { grid-template-columns: repeat(2, 1fr); }
+  .capability { min-height: 260px; }
+  .capability + .capability { border-left: 0; }
+  .capability:nth-child(even) { border-left: 1px solid var(--line); }
+  .capability:nth-child(n+3) { border-top: 1px solid var(--line); }
+  .capability h3 { margin-top: 36px; }
   .focus-wrap { padding-bottom: 92px; }
   .focus-panel { grid-template-columns: 1fr; gap: 32px; }
   .focus-items { border-left: 0; border-top: 1px solid var(--line); padding-top: 24px; }
@@ -314,6 +345,7 @@ html[data-theme="light"] .icon-moon { display: block; }
 }
 
 @media (max-width: 780px) {
+  body.nav-open { overflow: hidden; }
   .nav-contact { display: none; }
   .menu-toggle { display: grid; }
   .nav-links {
@@ -364,6 +396,11 @@ html[data-theme="light"] .icon-moon { display: block; }
   .case-facts div { grid-template-columns: 1fr; gap: 5px; }
   .case-metrics { grid-template-columns: 1fr; }
   .case-metrics div + div { border-left: 0; border-top: 1px solid var(--line); }
+  .approach-flow { flex-wrap: wrap; row-gap: 8px; margin-top: -4px; }
+  .capability-grid { grid-template-columns: 1fr; }
+  .capability { min-height: 230px; }
+  .capability:nth-child(even) { border-left: 0; }
+  .capability + .capability { border-top: 1px solid var(--line); }
   .focus-wrap { padding-block: 18px 72px; }
   .focus-panel { padding: 22px; }
   .focus-items { grid-template-columns: 1fr; }
